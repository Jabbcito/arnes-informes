# Routing del arnés

Este es el único archivo que se lee completo al empezar. Desde aquí se decide qué otro archivo abrir según la tarea — nunca se cargan todos los archivos del arnés de una vez.

Antes de esto, en un proyecto nuevo: `node nucleo/comun/herramientas/inicializar_proyecto.js` (ver "Primer paso, siempre" en `AGENTS.md`) — crea las carpetas de trabajo (`insumos/`, `fuentes/`, `output/...`) si aún no existen.

Enfoque actual del arnés: **tesis**. La carpeta `nucleo/informe/` reusa el contenido por sección de `nucleo/tesis/contenido/` (fuente única) con una tabla de omisiones propia (ver "Estado" al final).

## Reglas de comportamiento

Siempre, para cualquier tarea: `AGENTS.md` (raíz). Ahí están las 29 reglas no negociables (fuentes verificadas, no inventar, evidencia pendiente, no fabricar declaratorias/Turnitin, gate de instrumento/alfa, gate de datos principales, objetivos con opciones, dedicatoria preguntada, captura de insumos a disco, etc.) y el flujo completo paso a paso.

## Fuentes permitidas

`fuentes-permitidas.md` (raíz) — antes de investigar o citar cualquier cosa.

## Estructura general de la tesis (orden de secciones)

`nucleo/tesis/estructura-tesis-maestra.md` — solo cuando se necesita ver el documento completo o confirmar qué sección sigue. Para redactar una sección puntual, ir directo a su archivo en `nucleo/tesis/contenido/` (ver tabla abajo) — no hace falta abrir este archivo completo cada vez.

## Contenido por sección — tesis

No cargar todo `nucleo/tesis/contenido/`. Abrir solo el archivo que corresponde a la tarea actual:

| Si la tarea es... | Abrir |
|---|---|
| Analizar la rúbrica y preguntar los datos de autoría (autor, carrera, universidad, asesor) | `nucleo/skills/analizar-rubrica/SKILL.md` |
| Buscar fuentes (mínimo 15, ≤5 años, descargadas y leídas completas) | `nucleo/skills/investigar-fuentes/SKILL.md` |
| Definir/ajustar el título, verificar su disponibilidad, nombrar variables | `nucleo/tesis/contenido/titulo-y-variables.md` |
| Redactar objetivos (general y específicos, con opciones para elegir) | `nucleo/tesis/contenido/objetivos.md` |
| Redactar hipótesis (general y específicas, solo si el diseño las lleva) | `nucleo/tesis/contenido/hipotesis.md` |
| Crear/actualizar la matriz de consistencia (problema↔objetivo↔hipótesis↔variables↔metodología) | `nucleo/tesis/contenido/matriz-de-consistencia.md` |
| Redactar la Introducción | `nucleo/tesis/contenido/introduccion.md` |
| Redactar Antecedentes (marco teórico) | `nucleo/tesis/contenido/antecedentes.md` |
| Redactar Bases teóricas (marco teórico) | `nucleo/tesis/contenido/bases-teoricas.md` |
| Redactar Metodología (cualquier subsección: tipo/diseño, variables, población, procedimiento, análisis, aspectos éticos) | `nucleo/tesis/contenido/metodologia.md` |
| Calcular muestra (simple o estratificada), construir el instrumento, pilotear y validar con alfa de Cronbach (punto de control 3.4) | `nucleo/tesis/contenido/instrumento-y-muestra.md` |
| Decidir qué tipo de instrumento corresponde (encuesta, ficha de observación, lista de cotejo, ficha documental, guía de entrevista) | `nucleo/tesis/contenido/tipos-de-instrumento.md` |
| Recolectar y traer el dataset real de la muestra completa antes de Resultados (segundo punto de control) | `nucleo/tesis/contenido/recoleccion-datos-principal.md` |
| Generar cualquier tabla o figura (frecuencias, cruzadas, correlación, operacionalización, comparativas) | `nucleo/tesis/contenido/tablas-y-figuras.md` |
| Redactar Resultados (descriptivos o inferenciales) | `nucleo/tesis/contenido/resultados.md` |
| Redactar Discusión | `nucleo/tesis/contenido/discusion.md` |
| Redactar Conclusiones | `nucleo/tesis/contenido/conclusiones.md` |
| Redactar Recomendaciones | `nucleo/tesis/contenido/recomendaciones.md` |
| Redactar Resumen/Abstract y palabras clave | `nucleo/tesis/contenido/resumen-abstract.md` |
| Redactar Dedicatoria/Agradecimiento (preguntando a quién) | `nucleo/tesis/contenido/dedicatoria-agradecimiento.md` |
| Armar los Anexos | `nucleo/tesis/contenido/anexos.md` |

## Formato APA 7 (extraído de la guía oficial)

| Si la tarea es... | Abrir |
|---|---|
| Configurar formato del documento (márgenes, fuente, interlineado, niveles de título, portada) | `nucleo/comun/apa/formato-documento.md` |
| Escribir o revisar citas (narrativa/parentética, textuales, et al.) | `nucleo/comun/apa/citas.md` |
| Armar o revisar la lista de referencias | `nucleo/comun/apa/referencias.md` |
| Dar formato APA a tablas/figuras | `nucleo/comun/apa/tablas-figuras-apa.md` |

## Fórmulas y cálculos (regla 17: nunca calcular "de cabeza")

> Antes de correr cualquier script de esta sección: `node --version` primero (regla 20). Si falla, ver `nucleo/comun/herramientas/README.md` — nunca calcular a mano como solución de emergencia.

| Si la tarea es... | Abrir / correr |
|---|---|
| Decidir qué pruebas/fórmulas aplican al estudio | `nucleo/comun/formulas/elegir-diseno.md` |
| Escribir una fórmula en la Metodología o entenderla | `nucleo/comun/formulas/formulas-referencia.md` |
| Calcular tamaño de muestra (simple, estratificada, o modo interactivo) | `node nucleo/comun/herramientas/muestra.js --N ...` (agregar `--estratos "A:N,B:N"` o usar `--interactivo`) |
| Calcular alfa de Cronbach / KR-20 | `node nucleo/comun/herramientas/confiabilidad.js piloto.csv` |
| Generar tablas de frecuencias/cruzadas (variables categorizadas) | `node nucleo/comun/herramientas/descriptivos.js datos.csv ...` |
| Descriptivos de una variable numérica cruda (media, mediana, moda, DE) | `node nucleo/comun/herramientas/descriptivos-numericos.js datos.csv --columna ...` |
| Calcular correlación Pearson/Spearman | `node nucleo/comun/herramientas/correlacion.js datos.csv --x ... --y ...` |
| Calcular chi-cuadrado de independencia + V de Cramér | `node nucleo/comun/herramientas/chi-cuadrado.js datos.csv --x ... --y ...` |
| Calcular t de Student (2 grupos independientes) + d de Cohen | `node nucleo/comun/herramientas/prueba-t.js datos.csv --grupo ... --valor ...` |
| Calcular ANOVA de un factor (3+ grupos) + eta cuadrado | `node nucleo/comun/herramientas/anova.js datos.csv --grupo ... --valor ...` |
| Descargar una fuente (PDF/HTML) para leerla completa antes de registrar sus datos | `node nucleo/comun/herramientas/descargar_fuente.js <url> --salida fuentes/pdfs/nombre` |
| Generar el instrumento en HTML imprimible para el piloto en papel | `node nucleo/comun/herramientas/generar_instrumento_html.js output/trabajo/instrumento.md --salida output/entregables/instrumento.html ...` |
| Ver todos los scripts y sus límites | `nucleo/comun/herramientas/README.md` |

## Verificación y auditoría (antes de exportar)

| Si la tarea es... | Abrir |
|---|---|
| Verificar citas (código + APA) | `nucleo/skills/verificar-citas/SKILL.md` |
| Verificar estructura y formato | `nucleo/skills/verificar-formato/SKILL.md` |
| Auditoría integral pre-exportación | `nucleo/skills/auditar-tesis/SKILL.md` |
| Calcular muestra + construir instrumento + gate de alfa de Cronbach | `nucleo/skills/construir-instrumento/SKILL.md` |
| Exigir el dataset real de la muestra completa antes de Resultados (segundo gate) | `nucleo/skills/recolectar-datos-principales/SKILL.md` |
| Redactar objetivos con opciones | `nucleo/skills/definir-objetivos/SKILL.md` |
| Redactar Dedicatoria/Agradecimiento preguntando a quién | `nucleo/skills/redactar-preliminares/SKILL.md` |

## Exportación

| Si la tarea es... | Abrir |
|---|---|
| Exportar a Word (Pandoc + plantilla APA + numeración romana/arábiga) | `nucleo/comun/exportacion/exportar-word.md` |
| Exportar a PDF y verificarlo | `nucleo/comun/exportacion/exportar-pdf.md` |
| Crear la presentación de sustentación (HTML → PDF) | `nucleo/comun/exportacion/slides-html.md` y `nucleo/comun/exportacion/plantillas/slides-base.html` |

## Compatibilidad multi-herramienta

El arnés funciona en OpenCode (lee `AGENTS.md` nativo), Codex (también `AGENTS.md`) y Claude Code (entra por `CLAUDE.md`, que apunta aquí) — y en cualquier otro agente que pueda leer y seguir instrucciones en Markdown. Deliberadamente **no** hay carpetas propietarias (`.opencode/`, `.claude/`, `.codex/`): las skills viven en una sola carpeta neutra, `nucleo/skills/`, y se abren siguiendo esta tabla, no por un mecanismo de auto-descubrimiento de una herramienta específica.

## Informe (no-tesis)

`nucleo/informe/estructura-informe-maestra.md` — versión reducida de la tesis. El contenido detallado de cada sección sigue viniendo de `nucleo/tesis/contenido/` (fuente única); qué omitir o fusionar de cada archivo está explícito, archivo por archivo, en `nucleo/informe/contenido/omisiones.md` — no a criterio libre del agente.

## Skills

`nucleo/skills/README.md` — tabla de las 17 skills y qué cubre cada una. Abrir la skill puntual (`nucleo/skills/<nombre>/SKILL.md`) solo cuando se va a ejecutar ese paso.

## Cómo retomar una sesión

El arnés no tiene memoria propia entre sesiones — cada conversación nueva (aunque sea otra herramienta: OpenCode hoy, Claude Code mañana) empieza sin saber nada de la anterior. Esto es normal y esperado porque la tesis tiene **dos puntos donde el trabajo se pausa a esperar datos reales** que solo el alumno puede traer (el piloto y la recolección principal), y eso puede tardar días o semanas. La fuente de verdad siempre son los archivos del proyecto, nunca la conversación — por eso, al empezar cualquier sesión sobre un proyecto ya iniciado, antes de redactar nada:

1. Leer `insumos/` — ¿hay algo que el alumno compartió en una sesión anterior y que ya está guardado ahí (regla 27)? Si algo se mencionó en el chat pero no se ve un archivo correspondiente, no se asuma perdido sin más: se le pregunta al alumno y se guarda en ese mismo turno.
2. Leer `output/trabajo/brief.md` (¿ya está confirmado?).
3. Leer `output/trabajo/informe.md` completo: ¿dónde se detiene el contenido real? ¿qué `[EVIDENCIA PENDIENTE]` o `[PENDIENTE: ...]` quedan? Si el documento ya es extenso (varias decenas de páginas — tesis avanzada), no hace falta releer todo carácter por carácter en cada turno posterior de la misma sesión: releer la sección específica que se va a tocar (por título/número), y solo el documento completo al empezar la sesión, para no gastar contexto de más en secciones que no van a cambiar.
3.1. **Si el contenido de una sección no coincide con lo que el arnés generó la última vez** (aparece texto, una cifra o un párrafo que no sigue el estilo/estructura que el propio arnés habría escrito) — es indicio de que el alumno editó `informe.md` a mano fuera de la conversación. No sobrescribir esa sección en silencio: decírselo al alumno ("veo un cambio en esta sección que no reconozco de la última sesión, ¿lo hiciste tú? ¿lo mantengo o lo reemplazo?") antes de aplicar cualquier edición ahí. El arnés no tiene control de versiones — esta pregunta es la única red de seguridad contra perder una edición manual real.
4. Revisar si existen `output/trabajo/piloto.csv` y `output/trabajo/datos-principales.csv` — su sola presencia (o ausencia) dice en qué punto de control está el alumno:

| Situación | Qué significa | Dónde retomar |
|---|---|---|
| Metodología 3.4 sin alfa real registrado | Gate 1 no superado | `construir-instrumento` — no redactar nada más allá de 3.4 |
| 3.4 con alfa ≥ 0.70 pero sin `output/trabajo/datos-principales.csv` | Gate 1 superado, gate 2 pendiente | `recolectar-datos-principales` — no redactar Resultados con datos de relleno |
| `output/trabajo/datos-principales.csv` presente | Ambos gates superados | Seguir con Resultados en adelante, según dónde se haya quedado `informe.md` |

Esto no es un mecanismo nuevo, solo lo hace explícito: es el mismo principio que ya rige todo el arnés (regla 6, 8, 21/26 y 27 de `AGENTS.md`) — los archivos son la fuente de verdad, nunca la conversación. Verificar esto primero evita tres errores: re-preguntar algo que el alumno ya resolvió en otra sesión, saltarse un gate porque la conversación "no se acuerda" de que estaba pendiente, o perder un insumo que el alumno compartió y que nunca se guardó en disco.

## Estado de este arnés

- Completo y validado contra tesis reales: estructura, contenido por sección, fuentes, reglas, APA 7 (de la guía oficial), fórmulas por diseño, scripts probados con datos conocidos y pipeline de exportación verificado (Pandoc: tablas nativas + imágenes incrustadas + TOC).
- **Resuelto en v17**: `nucleo/informe/contenido/omisiones.md` — el modo informe ya no depende de que el agente decida en vivo qué omitir de `tesis/contenido/`; hay una tabla explícita archivo por archivo.
- Formato de skill verificado contra la documentación oficial de OpenCode, Claude Code y Codex (todas siguen el estándar abierto Agent Skills): frontmatter `name` (igual al nombre de la carpeta) y `description`. Las 17 skills del arnés viven en `nucleo/skills/<nombre>/SKILL.md`, una carpeta neutra sin mecanismo propietario de ninguna herramienta (v6).
- Verificado con impresión real (Edge headless `--print-to-pdf`, equivalente a Ctrl+P → Guardar como PDF): `nucleo/comun/exportacion/plantillas/slides-base.html` produce un PDF de 7 páginas, cada una 960×540pt (16:9 exacto, sin cortes ni páginas extra), con texto, acentos y color del título correctos. Pendiente solo un vistazo visual fino (franja de color, sombreados) en un visor de PDF completo — no se pudo confirmar al 100% por límites de la herramienta de captura usada aquí.
- Verificado con LibreOffice (abre y exporta DOCX de Pandoc sin errores): un documento con salto de sección y numeración diferenciada (`../herramientas/`-independiente; ver `nucleo/comun/exportacion/exportar-word.md`) exporta a PDF con la página de preliminares mostrando "i" y la primera página del cuerpo mostrando "1" — el mecanismo funciona. **Sigue pendiente reproducir el mismo paso con Word real** (esta máquina no lo tiene, tampoco la que se usó en v17) — desde v17 esto ya no es solo una nota interna: es el punto 12 de `auditar-tesis` (`[PENDIENTE: REVISIÓN EN WORD REAL]`), un chequeo humano explícito que el alumno debe confirmar antes de exportar.
- Pendiente de verificar: si `node` queda expuesto en el PATH de la terminal en las versiones de escritorio de OpenCode/Claude Code/Codex (probado solo en máquina de desarrollo con Node ya instalado, no en una instalación limpia de alumno). El chequeo `node --version` (regla 20) hace que esto falle de forma clara y con instrucciones en vez de romperse en silencio, pero no reemplaza la prueba real en una PC nueva. Desde v17, este chequeo es explícitamente el primer paso de cualquier sesión ("Primer paso, siempre" en `AGENTS.md`), antes incluso de `inicializar_proyecto.js` — recomendado como el "smoke test" del día 1 de la Sesión 1 del curso.
- **Probado de punta a punta con una tesis sintética completa** (título → matriz de consistencia → fuentes marcadas `[EVIDENCIA PENDIENTE]`, sin fabricar citas reales → metodología con `nucleo/comun/herramientas/muestra.js`/`nucleo/comun/herramientas/confiabilidad.js` sobre datos simulados n=60/piloto n=15 → resultados con `nucleo/comun/herramientas/descriptivos.js`/`nucleo/comun/herramientas/correlacion.js` → discusión → auditoría con `nucleo/comun/herramientas/verificar_citas.js`/`nucleo/comun/herramientas/verificar_estructura.js` en verde → exportación real a DOCX vía Pandoc + `nucleo/comun/exportacion/plantillas/plantilla-apa.docx` (3 tablas nativas, imagen de anexo incrustada, índice como campo, estilos aplicados) → PDF vía LibreOffice (8 páginas, contenido íntegro) → slides HTML rellenadas con el mismo contenido → PDF de sustentación (7 páginas, 16:9 exacto)). **Esta prueba encontró y corrigió un bug real**: `nucleo/comun/herramientas/verificar_citas.js` no detectaba apellidos españoles con partícula ("de la Cruz", "Del Pozo") — corregido y re-verificado sin regresión contra el caso de prueba original. (Nota histórica corregida en v17: esta misma entrada decía que los apellidos compuestos SIN partícula, ej. "Vargas Llosa", solo se detectaban por su primer término — eso se corrigió en v8 y la nota nunca se actualizó; probado de nuevo en v17 con "Vargas Llosa" y "Angarita Becerra" en un caso real, ambos se detectan y emparejan correctamente. Ver `../herramientas/README.md` para las limitaciones que sí siguen vigentes.)
- No verificado visualmente (limitación de la herramienta de captura, no evidencia de fallo): cómo se ve exactamente una tabla larga partida en un salto de página del PDF exportado. Es estructuralmente una tabla OOXML única con fila de encabezado repetida — comportamiento que la guía APA real recomienda explícitamente — pero no se confirmó con una captura visual. Desde v17, esto también forma parte del punto 12 de `auditar-tesis` (revisión visual en Word real) en vez de quedar solo como nota interna.
- **v3 — verificado por código**: los saltos de página reales entre preliminares (Declaratorias, Dedicatoria, Agradecimiento, Resumen, Abstract) funcionan con `pandoc --from markdown+raw_attribute` + bloque ```` ```{=openxml}<w:p><w:r><w:br w:type="page"/></w:r></w:p>``` ```` — probado con 3 secciones → 3 páginas reales en el PDF (LibreOffice). Las Referencias envueltas en `::: {custom-style="Bibliography"} :::` sí aplican el estilo con sangría francesa real (confirmado por XML) y conservan la cursiva de los títulos. El caption nativo de Pandoc para tablas (`Table: **Tabla N.** *Título*`) y figuras (`![**Figura N.** Título](ruta)`) conserva negrita/cursiva y genera estilos distintivos (`TableCaption`/`ImageCaption`) que permiten un índice de tablas/figuras real en Word (Referencias → Insertar tabla de ilustraciones → Estilo) — sin numeración automática (se escribe a mano, igual que siempre).
- **v3 — nuevo punto de control**: no se redacta más allá de Metodología 3.4 sin muestra calculada + instrumento construido + piloto real + alfa de Cronbach ≥ 0.70 (`nucleo/tesis/contenido/instrumento-y-muestra.md`, skill `construir-instrumento`). Los objetivos ahora se presentan como opciones (skill `definir-objetivos`) y la Dedicatoria/Agradecimiento preguntan a quién van dirigidos (skill `redactar-preliminares`) — estas 3 son reglas de comportamiento del agente, no verificables por script; la verificación es de que el texto de la instrucción sea claro y esté en el lugar correcto del flujo.
- **v4 — verificado por código**: `nucleo/comun/herramientas/muestra.js` ahora soporta muestreo estratificado (`--estratos "A:N,B:N,..."`, afijación proporcional, probado a mano contra N=120 en 3 estratos: n=92 repartido en 31/27/34, suma exacta) y modo `--interactivo` (probado con entradas simuladas por stdin, produce el mismo resultado que el modo por flags). El modo interactivo usa un lector de líneas propio en vez de `readline.question()` encadenado porque ese patrón pierde preguntas cuando stdin no es una terminal interactiva (bug real encontrado durante la prueba, corregido y verificado sin regresión).
- **v4 — segundo punto de control**: no se redacta Resultados, Discusión, Conclusiones ni Recomendaciones sin `output/trabajo/datos-principales.csv` real — el dataset de aplicar el instrumento ya validado a toda la muestra, nunca el CSV del piloto (`nucleo/tesis/contenido/recoleccion-datos-principal.md`, skill `recolectar-datos-principales`, regla 26). Igual que el gate del piloto, es una regla de comportamiento del agente, no verificable por script.
- **v4 — objetivos en dos etapas**: la skill `definir-objetivos` ya no genera general y específicos juntos; espera a que el alumno confirme el general antes de generar los específicos, y estos quedan ligados a las variables/dimensiones del general elegido (no a una lista genérica). Ver `nucleo/tesis/contenido/objetivos.md`.
- **v4 — tipo de instrumento antes del formulario**: `construir-instrumento` ya no asume "encuesta Likert" por defecto — primero determina (o pregunta) qué tipo de instrumento corresponde al diseño declarado (encuesta, prueba, ficha de observación, lista de cotejo, ficha documental, guía de entrevista/focus group), cada uno con su propia escala y su propio coeficiente de confiabilidad (o ninguno, en documental/cualitativo). Ver `nucleo/tesis/contenido/tipos-de-instrumento.md`.
- **v5 — separación común/tesis**: `nucleo/comun/` (APA, fórmulas, herramientas, exportación) salió de `nucleo/tesis/` porque no es exclusivo de tesis — `nucleo/tesis/` quedó solo con `contenido/` y `estructura-tesis-maestra.md`.
- **v6 — skills agnósticas**: `nucleo/skills/` es la única carpeta de skills, sin `.opencode/`, `.claude/` ni `.codex/` — cualquier IA las lee como Markdown siguiendo esta tabla.
- **v7 — todo lo generado vive en `output/`**: `output/trabajo/` (borradores, reportes, checklist, piloto y datos principales) y `output/entregables/` (DOCX/PDF/slides finales) — antes eran `trabajo/`/`entregables/` sueltos en la raíz del proyecto. `consigna/` se generalizó a `insumos/` y ya no es solo para la rúbrica: la nueva regla 27 exige que cualquier material que el alumno comparta en el chat se guarde a disco en el mismo turno (`insumos/`, `fuentes/`, `anexos/imagenes/` o `output/trabajo/` según el tipo) — nunca se deja solo en el contexto de la conversación.
- **v8 — probado de punta a punta con una tesis real de prueba, corrida en la misma sesión de IA** (no simulada como en la ronda #18): el gate de instrumento/alfa bloqueó de verdad (α=0.633) y se desbloqueó tras reformular ítems (α=0.974); el gate de datos principales exigió el CSV real antes de Resultados; exportación real a DOCX/PDF con Pandoc + LibreOffice, verificada por inspección de XML (6 saltos de página, sangría francesa real, captions de tabla). Esta prueba encontró y corrigió 2 bugs reales en `nucleo/comun/herramientas/verificar_citas.js`: apellidos compuestos sin partícula (ej. "Angarita Becerra", "Pacheco Olguín") se truncaban de forma inconsistente entre el texto y las Referencias, y `fuentesVerificadas()` nunca lograba emparejar el campo `**Autor**:` real de `fuentes/investigacion.md`. Ambos corregidos y re-verificados.
- **v9 — fuentes reales desde el texto completo, no desde el resumen del buscador** (regla 28, skill `investigar-fuentes` reescrita en 2 fases): mínimo 15 fuentes ≤5 años de antigüedad, repartidas por categoría (definición conceptual, dimensión, teoría, antecedente empírico) — si no se llega en 5 años se amplía a 10 documentándolo, y si aun así no alcanza, se pregunta al alumno. Cada fuente se descarga con el nuevo `nucleo/comun/herramientas/descargar_fuente.js` (sin dependencias, sigue redirecciones, detecta PDF/HTML por Content-Type) y se lee completa antes de registrar sus datos — nunca se completan país/diseño/muestra/definición solo con lo que muestra un buscador. `fuentes-permitidas.md` se amplió con 6 bases de acceso abierto (DOAJ, CORE, BASE, Semantic Scholar, ERIC, Latindex) y 6 repositorios universitarios peruanos más. `nucleo/skills/analizar-rubrica` ahora pregunta los datos de autoría (autor, carrera, universidad, asesor) en el mismo paso del brief — hueco real confirmado por exploración: la carátula los exige pero nada en el flujo los pedía. Nuevo `nucleo/comun/herramientas/generar_instrumento_html.js` genera una versión imprimible del instrumento (encabezado institucional, consentimiento, ítems con casillas 1-5) — probado con el `instrumento.md` real de la prueba v8, produce un PDF de 2 páginas vía LibreOffice. Regla 29 nueva: todo lo que el arnés genera vive dentro de `output/`, sin excepción — reforzada con un valor por defecto en `generar_instrumento_html.js` (si no se pasa `--salida`, cae solo en `output/entregables/`).
- **v9.1 — dos bugs reales encontrados por captura de pantalla del usuario, corregidos y verificados**: (1) tablas anchas (operacionalización, matriz de consistencia) exportaban con columnas ilegibles — solución real: sección en horizontal automática vía OOXML (dos bloques `{=openxml}` con `sectPr`, mismo mecanismo que los saltos de página) + reducir la tabla de operacionalización a 4 columnas, moviendo las definiciones a prosa (ver `nucleo/comun/exportacion/exportar-word.md`, "Tablas anchas"); verificado con Pandoc+LibreOffice, la página de la tabla sale en 792×612pt mientras el resto del documento queda en 612×792pt. (2) Las Referencias no tenían sangría francesa real pese a estar documentado: el estilo `Bibliography` de `nucleo/comun/exportacion/plantillas/plantilla-apa.docx` tenía `<w:pPr />` vacío — nunca tuvo la sangría configurada. Corregido directamente en la plantilla (720 twips de sangría francesa + interlineado doble) y confirmado visualmente en un PDF de prueba.
- **v10 — fidelidad de datos + cobertura estadística inspirada en SPSS**: `muestra.js`, `confiabilidad.js`, `descriptivos.js` y `correlacion.js` ahora aceptan `--salida <archivo>` — el resultado se escribe a un archivo además de imprimirse, para copiarlo de ahí a `informe.md` y nunca retipearlo de memoria (regla 29, riesgo real de error de transcripción que ningún verificador detectaba). Nuevo `lib-salida.js` (helper compartido) y `lib-stats.js` ampliado con `median`, `mode`, `standardDeviation`, `pF` (distribución F, reutiliza la beta incompleta ya existente) y `pChiCuadrado` (función gamma incompleta regularizada, mismo estilo Numerical Recipes que ya usaba `betai`). Cuatro scripts nuevos, cada uno validado contra un ejemplo con resultado exactamente conocido: `chi-cuadrado.js` (χ²=16.667, gl=1, ejemplo de manual), `prueba-t.js` (Welch, t=-1.861/gl=17.78 contra el dataset `sleep` de R), `anova.js` (F=4.846/p=0.0159 contra el dataset `PlantGrowth` de R), `descriptivos-numericos.js` (media/mediana/moda/DE contra cálculo a mano). Salida de los tres primeros al estilo SPSS Custom Tables: estadístico + gl + p-valor + tamaño del efecto (V de Cramér / d de Cohen / eta cuadrado) juntos en el mismo bloque. Shapiro-Wilk, Kolmogórov-Smirnov, Wilcoxon, U de Mann-Whitney, Kruskal-Wallis, t pareada, post-hoc de ANOVA y regresiones siguen derivados a SPSS/Jamovi a propósito (riesgo de bug silencioso más alto que el beneficio). También: nuevo `nucleo/tesis/contenido/hipotesis.md` (hueco real: el flujo mencionaba hipótesis pero no tenía archivo propio), Resumen/Abstract agregado al "Flujo obligatorio" de `AGENTS.md` (tenía contenido pero no aparecía en la lista), y corrección del conteo de reglas en esta misma sección (decía 27/28, ya eran 29).
- **v11 — todo el motor movido a `nucleo/`, para que el alumno solo vea sus propias carpetas**: `comun/`, `tesis/`, `informe/` y `skills/` (con todo su contenido) se movieron a `nucleo/comun/`, `nucleo/tesis/`, `nucleo/informe/`, `nucleo/skills/` — antes vivían sueltos en la raíz del proyecto, mezclados visualmente con `insumos/`, `fuentes/`, `anexos/` y `output/` (las carpetas que el alumno sí usa). En la raíz solo quedan los `.md` de entrada (`AGENTS.md`, `ROUTING.md`, `CLAUDE.md`, `fuentes-permitidas.md`), `nucleo/` y las carpetas de trabajo. `nucleo/comun/herramientas/inicializar_proyecto.js` (v10.1, sin ronda propia) no necesitó cambios — sigue creando las carpetas relativas al directorio desde el que se invoca el arnés (la raíz), que es exactamente donde queda `AGENTS.md` tras el movimiento. Se corrigieron también dos referencias que ya estaban rotas antes de esta ronda (comandos `node ../../comun/...` en `investigar-fuentes` y `generar-indice-y-referencias`, que nunca podían resolver bien porque una ruta de terminal se resuelve contra el directorio de trabajo, no contra la ubicación del `.md` que la menciona) — ahora son rutas relativas a la raíz (`node nucleo/comun/...`), consistentes con el resto del arnés. Verificado: script de chequeo de enlaces cruzados sobre todo el árbol → `OK: sin rutas rotas`; `node --check` sobre los 16 `.js` movidos; prueba real en una copia temporal (`inicializar_proyecto.js` + `muestra.js` corridos desde la raíz) confirmando que `insumos/`, `fuentes/`, `anexos/`, `output/` quedan al mismo nivel que `AGENTS.md`, fuera de `nucleo/`.
- **v11.1 — prueba E2E real del movimiento a `nucleo/`, sobre un clon fresco de GitHub**: corridos de verdad (no solo `--check`) los 8 scripts de cálculo, los 2 verificadores, `descargar_fuente.js` contra una URL real, `generar_instrumento_html.js`, y una exportación Pandoc real con la plantilla movida — todos funcionaron. Esta prueba encontró 3 bugs reales que la verificación anterior (basada en regex sobre rutas con prefijo de carpeta conocido) no detectó por revisar solo `.md`: (1) tres strings de usuario dentro de `.js` (`verificar_citas.js`, `verificar_estructura.js`, `generar_instrumento_html.js`) seguían mencionando `comun/apa/...` sin el prefijo `nucleo/` — visibles en el reporte de citas y en el pie del instrumento HTML generado; (2) `investigar-fuentes/SKILL.md` apuntaba a `fuentes-permitidas.md` con una profundidad de `../` insuficiente (nunca se ajustó porque el barrido anterior solo tocó referencias a `AGENTS.md`/`ROUTING.md`); (3) el propio ajuste de profundidad de la ronda anterior sobre-corrigió `skills/README.md` y `tesis/estructura-tesis-maestra.md` (que están a un nivel, no dos, dentro de `nucleo/`), dejándolos con un `../` de más. Los tres corregidos y reverificados con un script de chequeo de enlaces más amplio (cualquier ruta con `../`, no solo las que empiezan con un nombre de carpeta conocido) → `OK: sin rutas rotas`.
- **v12 — prueba E2E completa de una tesis de principio a fin (mismo tema que v8), en una copia clonada fresca**: se siguió el flujo completo como lo haría un alumno real — brief sin rúbrica, datos de autoría, verificación de disponibilidad del título (encontró títulos casi idénticos ya registrados en ALICIA y obligó a diferenciar el título), fuentes reales descargadas y leídas (16 URL intentadas, 4 fallaron por bloqueo/timeout — dos de ellas, USIL y Autónoma, devolvieron HTTP 200 con una página de verificación anti-bot en vez del artículo real, detectado solo porque se leyó el archivo completo en vez de confiar en el código de estado; no se llegó a la cuota de 15 ni ampliando a 10 años, así que se activó por primera vez en una prueba real la rama de "detente y pregunta al alumno" de `investigar-fuentes`, y el alumno confirmó seguir con 11), objetivos con opciones, hipótesis, matriz de consistencia, Gate 1 (primer piloto con datos técnicos aleatorios dio α=0.342 y bloqueó correctamente; ítems reformulados, segundo piloto dio α=0.807 y desbloqueó), Gate 2 (dataset de n=19 real), resultados con `correlacion.js` (rho=0.848), exportación real a DOCX. Encontró y corrigió 2 bugs reales adicionales: (1) `verificar_citas.js` exigía que cada línea `**Autor**:` de `fuentes/investigacion.md` empezara con un guion de lista (`- **Autor**:`), un formato que ninguna skill documenta — cualquier agente que escribiera el archivo sin ese guion (como ocurrió en esta misma prueba) hacía que el verificador nunca encontrara ninguna fuente registrada, sin ningún error visible; el guion ahora es opcional en el parser. (2) Documentado (no corregido en código, por ser una ambigüedad real del idioma) que `verificar_citas.js` da un falso positivo cuando la preposición "de" antecede a una cita narrativa por razones gramaticales ("consistente con el antecedente de Muñoz-Vargas et al., 2025") — el script no puede distinguir esa "de" de un apellido real con partícula ("de la Cruz"), así que se dejó como límite honesto documentado en `comun/herramientas/README.md` con la solución práctica (redactar sin anteponer "de"). El verificador de citas también atrapó dos errores reales de la propia redacción de esta prueba (una cita textual sin página de más de 15 palabras y una cita directa a una fuente secundaria nunca leída) — funcionando exactamente como debía. Quedó sin completar por alcance de sesión (no por fallo del arnés): Resumen/Abstract, Anexos, Dedicatoria/Agradecimiento, auditoría integral y slides — el resto del flujo (los dos gates, fuentes, exportación) se validó de punta a punta.
- **v13 — prueba E2E sin omitir ningún paso, con los 4 entregables reales**: se retomó el mismo tema y datos de v12, pero esta vez se completó el flujo entero (pasos 0-20 de `AGENTS.md`) sin detenerse antes de tiempo — carátula, declaratorias marcadas `[PENDIENTE: FIRMA...]`, Dedicatoria/Agradecimiento preguntados de verdad al usuario (no genéricos), Resumen/Abstract, Anexos (4, referenciados desde el cuerpo), auditoría integral con `checklist-final.md` real (10/11 puntos en verde; el punto 11 quedó honestamente en rojo porque el propio alumno de la prueba no conocía el apellido de su asesor ni su línea de investigación — se le preguntó cómo seguir y decidió exportar igual, dejando esos dos datos marcados como pendientes, tal como pasaría con un alumno real), y los 4 entregables finales: DOCX, **PDF real del informe** (vía LibreOffice, `soffice.exe` con ruta completa cuando no está en el PATH — antes se había reportado "no disponible" cuando solo faltaba encontrar el ejecutable) y **HTML + PDF real de las slides** (vía Edge headless `--print-to-pdf`, 7 páginas verificadas). Se buscaron fuentes reales adicionales y esta vez sí se alcanzó la cuota completa de 15 (10 dentro de 5 años + 3 ampliadas a 10 + 1 de la misma universidad/ciudad del proyecto — un antecedente casi idéntico encontrado en UCV Trujillo, que obligó a diferenciar el título también en esta ronda). No se encontraron bugs nuevos en el arnés: los 2 problemas que aparecieron durante la redacción (una cita a un apellido con guion que no coincidía con el registrado sin guion, y un encabezado "Referencias" con negrita manual redundante que no coincidía con el detector de secciones) fueron errores propios de esta sesión al redactar, no del código — los verificadores los atraparon correctamente y se corrigieron en el borrador, no en el arnés. Se confirmó y documentó en `comun/exportacion/exportar-pdf.md` y `slides-html.md` que el arnés **no depende de Word**: LibreOffice (gratuito) es la ruta principal para DOCX→PDF, y Edge/Chrome headless para las slides — ambos con el comando exacto verificado, incluyendo la ruta completa al ejecutable para cuando no está en el PATH de la terminal.
- **v14 — bug real de tablas ilegibles reportado con captura de pantalla, corregido en la plantilla de exportación**: la tabla de operacionalización de variables del DOCX salía con columnas descuadradas y texto con aspecto de sangría irregular ("está hasta el perno", en palabras del usuario). Causa raíz encontrada inspeccionando el XML del DOCX generado (afectaba a **toda** tabla del documento, no solo a esa): (1) Pandoc reparte el ancho de columnas de una pipe table siempre en partes iguales, ignorando el contenido — verificado comparando el `<w:tblGrid>` resultante de separadores `---` parejos contra separadores con dashes muy desiguales, salió idéntico en ambos casos; (2) toda celda de tabla usa el estilo de párrafo `Compact`, que en `plantilla-apa.docx` heredaba sangría de primera línea (1.27 cm) e interlineado doble de `BodyText` — correcto para párrafos de cuerpo, pero dentro de una celda angosta la sangría de la primera línea (que las líneas siguientes no tienen, por el wrap forzado del punto 1) es justo lo que se veía como "unas centradas, otras con espacio al inicio"; (3) no había bordes reales de tabla completa (solo bajo el encabezado) y el encabezado nunca quedaba centrado pese a que `comun/apa/tablas-figuras-apa.md` ya lo pedía. Corrección verificada exportando con Pandoc + convirtiendo a PNG con LibreOffice, antes/después comparado visualmente contra la tabla real del reporte: nuevo `comun/exportacion/plantillas/tabla-anchos.lua` (filtro Lua de Pandoc que mide el contenido real de cada columna y fija anchos proporcionales, mínimo 12%/máximo 45% por columna, agregado al comando base en `exportar-word.md`) + `plantilla-apa.docx` parcheada (estilo `Compact` sin sangría ni interlineado doble; estilo de tabla `Table` con borde superior e inferior reales y encabezado centrado de verdad vía formato condicional de tabla). Probado sin regresión en tablas de 2, 5 (frecuencias cortas y parejas) y 6 columnas — la de 6 columnas sigue necesitando la guía ya existente de "Tablas anchas" (orientación horizontal), que no se tocó y sigue siendo complementaria.
- **v16 — auditoría comparativa contra herramientas open source, con 4 adopciones reales** (a pedido explícito del usuario, que quería no reconstruir desde cero lo que ya existiera maduro): se comparó cada área del arnés contra el ecosistema open source (búsqueda de fuentes, lectura de PDF, validación de instrumentos, cálculo estadístico, exportación a Word/PDF/PPT, y los "arneses" completos comparables como GPT Researcher/STORM/OpenDraft — Python, arquitectura incompatible con "solo Node + la IA"). Resultado: la mayoría del código propio (validación de instrumentos v15, estadística general) ya está verificado contra resultados exactos y no tiene equivalente OSS real al cual migrar; se adoptaron 4 piezas donde sí había ganancia real, todas invocables por script (sin apps con interfaz propia), respetando la restricción de que el arnés solo dependa de Node.js + la IA del alumno — esto introdujo, por primera vez, dependencias npm reales (`package.json` nuevo en la raíz; `node_modules/` ya estaba en `.gitignore` desde v5): (1) **`buscar_fuentes.js`** (OpenAlex, gratis, sin clave, sin dependencia npm — HTTPS nativo) cierra el problema real de páginas anti-bot encontrado en la prueba E2E v12 (probado con búsqueda real: 19 resultados reales sobre "procrastinación académica universitarios", con DOI/PDF de acceso abierto verificados); (2) **`verificar_doi.js`** (CrossRef, gratis, sin clave) como chequeo de refuerzo de que un DOI existe — probado contra un DOI real, metadatos coincidieron exactamente con OpenAlex; (3) **`descargar_fuente.js` extendido con `pdf-parse`** (Apache-2.0): ahora extrae SIEMPRE el texto de todo PDF descargado a un `.txt` hermano, para que la lectura completa no dependa de si la herramienta de IA del alumno (OpenCode/Claude Code/Codex) sabe abrir PDF nativamente — pedido explícito del usuario tras un round de discusión sobre este gap; probado con un PDF real (21,363 caracteres extraídos correctamente), con aviso claro (no fallo silencioso) si el PDF es escaneado y no tiene texto extraíble; (4) **`generar_slides_pptx.js`** (PptxGenJS, MIT): genera un `.pptx` real y editable parseando la `presentacion.html` ya revisada (no `slides.md`, para que nunca diverja de lo aprobado) — cierra un vacío reconocido desde hacía varias rondas ("PPTX opcional avanzado", nunca construido); probado generando 7 diapositivas reales desde la plantilla estándar y confirmando visualmente (exportado a PDF/PNG con LibreOffice) título, viñetas con negrita parcial, cifra destacada, callout con borde de acento y tabla con encabezado en negrita, todo con los colores `--primario`/`--acento` correctos. Se descartó explícitamente CORE API (exige clave, fricción extra), la migración del cálculo estadístico general a `jStat`/`stdlib` (sin ganancia real sobre código ya verificado contra R) y la migración de citas/referencias a Pandoc citeproc + CSL APA 7 (mejora real identificada pero dejada en backlog, no bloqueante para el lanzamiento del curso).
- **v18 — apellido compuesto con partícula EN MEDIO, gap real distinto al de v17**: el usuario corrigió que había dos tipos de apellido compuesto en español, y v17 solo probó uno. Confirmado empíricamente (reconstruyendo las regex reales del archivo sin retipearlas, contra casos concretos): "Vargas Llosa"/"Angarita Becerra" (sin partícula) y "de la Cruz"/"de Mesa" (partícula al inicio) ya funcionaban; "Sandoval de Castilla" (partícula EN MEDIO, dos palabras propias unidas por "de") fallaba de verdad — la cita parentética `(Sandoval de Castilla, 2020)` no se detectaba en absoluto, y la narrativa `Sandoval de Castilla (2020)` capturaba solo `"de Castilla"`, perdiendo `"Sandoval"` (el apellido extraído no calzaba con la fuente real, dando un falso "sin entrada en Referencias"). También se confirmó que apellido paterno=materno idéntico (`García García`) y campos `Autor` con varios nombres de pila ya funcionaban sin cambios. **Corregido**: `AUTOR_BASE` en `nucleo/comun/herramientas/verificar_citas.js` ahora incluye una tercera alternativa `PALABRA + CONECTOR + PALABRA`, probada con la batería completa (los 8 casos, incluidos todos los ya validados en v17) sin ninguna regresión, y verificada de punta a punta con un `informe.md`/`investigacion.md` de prueba real (2 citas de "Sandoval de Castilla", 0 problemas detectados). Riesgo aceptado documentado en el código y en `README.md`: la misma ambigüedad ya conocida del patrón "de al inicio" en narrativa libre (frase genérica indistinguible de apellido real) se extiende también al patrón de partícula en medio — no se resuelve con una lista blanca por ser un cambio de arquitectura desproporcionado para este caso.
- **v17 — auditoría completa a pedido del usuario + salvaguarda de título**: el usuario reportó que al reformular un título no disponible, el arnés debía preservar las variables/enfoque de investigación (si no, se pierde el trabajo ya invertido en fuentes/matriz) — confirmado como gap real: el protocolo de disponibilidad ya existía (regla 13) pero ponía "enfoque" y "segunda variable" en la misma lista que "población/lugar/periodo" sin distinguir que los dos primeros no tocan el objeto de estudio y los dos últimos sí; el único chequeo de coherencia (`auditar-tesis`) llegaba al final, después de poder haber invertido trabajo real en variables desalineadas (se disparó de verdad en v12 y v13 sin quedar documentado si hubo retrabajo). **Corregido**: `nucleo/tesis/contenido/titulo-y-variables.md` y la skill `definir-titulo-variables` ahora separan ejes seguros (población/lugar/periodo, usar primero) de ejes que cambian el objeto de estudio (enfoque/segunda variable, solo si es imprescindible) — y si se usa un eje riesgoso con fuentes/matriz ya construidas, el arnés se detiene de inmediato a revisar con el alumno, en vez de esperar a la auditoría final. Además, a pedido del usuario, se hizo una auditoría amplia de todo el arnés y se cerraron los gaps encontrados con evidencia: (1) **`nucleo/informe/contenido/omisiones.md`** (nuevo) reemplaza la instrucción vaga "omite lo que la estructura reducida ya descarta" por una tabla explícita, archivo por archivo de `tesis/contenido/`, de qué omitir/fusionar en modo informe. (2) **Apellidos compuestos sin partícula, investigado y descartado como gap real**: una nota de la v3 decía que "Vargas Llosa" solo se detectaba por su primer término — se probó `verificar_citas.js` contra un caso real con "Vargas Llosa" y "Angarita Becerra" y ambos se detectan y emparejan correctamente (el bug se había corregido en v8, pero la nota nunca se actualizó); se corrigió el registro histórico, no el código, que ya funcionaba. (3) **Plagio/similitud**: nuevo `nucleo/comun/apa/originalidad-y-similitud.md` con guía de autocuidado (paráfrasis real vs. sinónimos, qué hacer si un reporte de Turnitin vuelve alto), referenciado desde `metodologia.md`. (4) **Ética real de comité institucional**: `metodologia.md` ahora pregunta explícitamente si el estudio involucra población vulnerable/menores/datos de salud, con marcador `[PENDIENTE: DICTAMEN COMITÉ DE ÉTICA]` (mismo patrón que Turnitin) y nueva fila en `anexos.md`. (5) **Diseño factorial / 2+ variables independientes**: nueva rama en `elegir-diseno.md` (ANOVA factorial/MANOVA), derivado a SPSS/Jamovi a propósito, mismo criterio honesto que post-hoc/AFE. (6) **Levantamiento de observaciones post-sustentación**: nuevo `nucleo/tesis/contenido/levantamiento-observaciones.md` + paso 21 opcional en el flujo de `AGENTS.md` — cómo registrar observaciones reales del jurado, aplicarlas solo a las secciones afectadas, y distinguir versión de sustentación de versión final. (7) **Ediciones manuales del alumno y documentos largos**: reforzado "Cómo retomar una sesión" en este mismo archivo — preguntar antes de sobrescribir contenido que parezca editado a mano fuera de la conversación, y releer por sección (no el documento completo) cuando ya es extenso. (8) **Verificación visual reforzada**: la tabla partida en salto de página y el flujo con Word real (ninguno de los dos se pudo cerrar en este entorno — sin Word real disponible) pasaron de nota interna a punto 12 explícito de `auditar-tesis`, `[PENDIENTE: REVISIÓN EN WORD REAL]`, que el alumno debe confirmar antes de exportar. (9) El chequeo `node --version` pasó a ser explícitamente el primer paso de cualquier sesión, antes de `inicializar_proyecto.js` — recomendado como "smoke test" del día 1, ya que nunca se verificó en una instalación limpia de alumno (sigue siendo una verificación pendiente real, ahora más visible en vez de enterrada en la regla 20).
- **v15 — gap real reportado por el usuario: faltaba el juicio de expertos (V de Aiken) y la correlación ítem-total en el flujo de validación de instrumentos**, que saltaba directo de "redactar ítems" a "piloto → alfa de Cronbach". Confirmado por exploración del repo: no existía script ni contenido para juicio de expertos (la única mención era un párrafo huérfano en `comun/formulas/formulas-referencia.md` que además referenciaba mal `confiabilidad.js`, que nunca calculó V de Aiken); `confiabilidad.js` tampoco calculaba correlación ítem-total (se delegaba a SPSS con una nota de una línea). **Punto de integridad académica aclarado explícitamente con el usuario antes de construir nada**: pidió inicialmente que la IA "simulara" jueces generando calificaciones ficticias — se le explicó que eso viola la regla central del arnés (nunca inventar resultados/estadísticas/encuestas, la misma que ya rige fuentes y citas) y se acordó la alternativa correcta: el arnés genera la ficha de validación **en blanco de calificaciones** (con los ítems reales ya redactados, para que el juez tenga algo real que evaluar) y nunca genera ni estima las calificaciones — esas siempre son datos reales que el alumno transcribe de su asesor y jueces. Cambios: nuevo `nucleo/comun/herramientas/validez-contenido.js` (V de Aiken por ítem y global, gate configurable, default 0.80 — verificado a mano: ítem con calificaciones [4,4,3]/[2,2,2]/[4,3,4] entre 3 jueces de 4 categorías da V=0.889/0.333/0.889 exacto); `confiabilidad.js` ahora imprime también la correlación ítem-total corregida de cada ítem (r de Pearson contra el total de los demás ítems, aviso si r<0.30 — verificada contra un cálculo de Pearson independiente, coincide exacto); `generar_instrumento_html.js` con nuevo modo `--ficha-experto` (mismos ítems reales del instrumento, columnas en blanco de Claridad/Relevancia/Pertinencia + observaciones + datos del juez, probado generando una ficha real desde un `instrumento.md` de ejemplo y confirmando visualmente que no hay ninguna calificación pre-llenada). `pearson()` se extrajo de `correlacion.js` a `lib-stats.js` para reutilizarla sin duplicar código. Se actualizó la secuencia completa de `nucleo/tesis/contenido/instrumento-y-muestra.md` y la skill `construir-instrumento` para insertar el nuevo paso 3 (juicio de expertos, gate V de Aiken ≥0.80) entre construir ítems y piloto, con el piloto y el gate de alfa (≥0.70) ahora explícitamente después. Se documentó el Análisis Factorial Exploratorio (AFE, validez de constructo) como paso opcional derivado a SPSS/Jamovi/R — mismo criterio que normalidad/rangos/post-hoc/regresiones, ya establecido en v10 (`comun/herramientas/README.md`, "Límite honesto"): no se implementa en código para no arriesgar un bug silencioso en un análisis estadístico complejo. CVR de Lawshe tampoco se implementó (decisión explícita del usuario: solo V de Aiken).
